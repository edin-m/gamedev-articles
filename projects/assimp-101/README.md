# Simple assimp loader

Load suzzane obj file.

```sh
$ cd <git-root>/bin
$ ./assimp-101 --use-indicies     # pack vertices and send indices array
$ ./assimp-101 --no-use-indicies  # send only vertices to GPU
```

